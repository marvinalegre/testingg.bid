services:
  www:
    image: nginx:alpine
    container_name: www
    restart: unless-stopped
    networks:
      - web-traffic
    volumes:
      - ./www:/usr/share/nginx/html:ro
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.www.rule=Host(`www.testingg.bid`)"
      - "traefik.http.routers.www.entrypoints=websecure"
      - "traefik.http.routers.www.tls.certresolver=myresolver"

networks:
  web-traffic:
    external: true

